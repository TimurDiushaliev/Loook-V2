import 'package:flutter_bloc/flutter_bloc.dart';

class JumpToCategorieIndexBloc extends Bloc<int, int>{
  JumpToCategorieIndexBloc(initialState) : super(initialState);

  
  @override
  Stream<int> mapEventToState(event) async*{
    switch(event){
      case 0:
      
    }
  }
}
