import 'package:flutter/material.dart';
import 'package:loook/widgets/app_bar/app_bar_title.dart';

class AdvertSuccessfullyAddedPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: AppBarTitle(),
        ),
        body: Stack(
          children: [
            //TODO:
            //carousel images
            //bottom sheet
          ],
        ),
      ),
    );
  }
}
