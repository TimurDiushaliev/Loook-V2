import 'package:flutter/cupertino.dart';

class ChatMessagesRepository {
  String messageContent;
  String messageType;
  ChatMessagesRepository({@required this.messageContent, @required this.messageType});
}