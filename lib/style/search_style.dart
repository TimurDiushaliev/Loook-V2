import 'package:flutter/material.dart';

class SearchStyle {
  static const searchInput = InputDecoration(
    labelText: 'Найти',
    labelStyle: TextStyle(color: Colors.black),
    filled: true,
    fillColor: Colors.white,
  );
}
