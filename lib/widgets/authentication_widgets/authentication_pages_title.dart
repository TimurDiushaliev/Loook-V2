import 'package:flutter/material.dart';

class AuthenticationPagesTitle extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Align(
      alignment: Alignment.center,
      child: Text(
        'ElSource',
        style: TextStyle(fontSize: 25, letterSpacing: 6),
      ),
    );
  }
}
